// TEMP: no auth yet — every write/read attributes to this seeded user.
// Single source of truth so replacing it (once auth lands) is one
// file, not a grep-and-hope across the codebase. See ADR-0004.
export const DEMO_USER_ID = 1;